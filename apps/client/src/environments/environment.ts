import { Environment } from "./env.interface";

export const environment: Environment = {
    production: true,
    apiPrefix: '/api',
    apiMockDelay: 0,
    version: '1.0.0',
    appName: 'ExpMan',
    credentials: null,
    defaultLanguage: 'en',
    authKey: 'expman-xxau-key'
};